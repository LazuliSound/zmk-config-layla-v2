if SHIELD_LAYLA_LEFT

    config ZMK_KEYBOARD_NAME
        default "LaylaV2"

    config ZMK_SPLIT_BLE_ROLE_CENTRAL
        default y

endif # LEFT


if SHIELD_LAYLA_LEFT || SHIELD_LAYLA_RIGHT

    config ZMK_SPLIT
        default y


if ZMK_DISPLAY

    config I2C
        default y

    config SSD1306
        default y

endif # ZMK_DISPLAY

endif # LEFT RIGHT